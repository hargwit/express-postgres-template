import { Router } from 'express'

/**
 * These routes contain the business logic of the server.
 * They should be logged and authenticated.
 */
export const appRouter = (): Router => {
    const router = Router()

    return router
}
